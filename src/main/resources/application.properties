spring.datasource.url=jdbc:postgresql://localhost:5432/${LOCAL_POSTGRES_DB}
spring.datasource.username=${LOCAL_POSTGRES_USER}
spring.datasource.password=${LOCAL_POSTGRES_PASSWORD}
spring.jpa.hibernate.ddl-auto=create-drop

webclient.connection.timeout=5000
webclient.response.timeout=5000
webclient.read.timeout=5000
webclient.write.timeout=5000